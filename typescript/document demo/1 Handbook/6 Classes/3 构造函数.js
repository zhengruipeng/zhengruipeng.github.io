//类构造函数与函数非常相似。您可以添加带有类型注释、默认值和重载的参数：
class Point2 {
    x;
    y;
    // Normal signature with defaults
    constructor(x = 0, y = 0) {
        this.x = x;
        this.y = y;
    }
}
class Point3 {
    constructor(xs, y) {
        // TBD
    }
}
